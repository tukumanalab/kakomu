# kakomu — 仕様書 (SPEC)

> アクリルスタンド／アクリルキーホルダーの入稿データ作成に特化した、クロスプラットフォームのベクターグラフィックエディタ。

- **バージョン**: 0.1 (draft)
- **最終更新**: 2026-09-09
- **ステータス**: プランニング（実装未着手）

---

## 1. 概要

### 1.1 何を作るか

Inkscape の「画像を配置して、その周囲をベジェ曲線で囲み、ベクターとして書き出す」というワークフローだけを切り出し、アクスタ／アクキー制作に必要な機能を足したデスクトップアプリケーション。

汎用ドローイングツールではなく、**「1枚の絵から1個のアクリルグッズの入稿データを作る」ための専用エディタ**として設計する。Inkscape の全機能を再実装することは目的としない。

### 1.2 名前の由来

「囲む」— 画像を囲むカットラインを作ることが、このツールの中心的な仕事であるため。

### 1.3 コアとなる価値

1. **背景透過からカットライン生成までが1本の導線に載っている** — 現状は「Photoshop/rembg で切り抜き → Illustrator/Inkscape でパス化 → 手作業でオフセット」と複数ツールをまたぐ必要がある。
2. **アクリルグッズ固有の寸法知識が組み込まれている** — 白フチ 3mm、キーホルダー穴 φ4mm、台座スリット 3.2mm といった数値を毎回調べ直さなくてよい。
3. **出力先ごとに正しい形式で出る** — カットラインはベクター SVG、印刷絵柄は原寸・高解像度・透過保持の PDF。

---

## 2. 想定ユーザーとユースケース

### 2.1 想定ユーザー

| ペルソナ | 説明 | 技術レベル |
|---|---|---|
| 個人創作者 | 自作イラストでアクスタを作りたい同人作家 | Illustrator は持っていない。Inkscape は「難しい」と感じている |
| 小規模事業者 | UV プリンタを自社導入し、受注制作しているショップ | 印刷知識はあるが、DTP ソフトのライセンス費を抑えたい |
| ホビイスト | 写真からアクキーを作りたい人 | 画像編集の経験がほぼない |

### 2.2 中心的なユースケース（ゴールデンパス）

```
1. 画像をドラッグ&ドロップでインポート
2. 「背景を消す」ボタン → AI が自動で切り抜き
3. 「カットラインを作る」ボタン → 3mm オフセットの輪郭が自動生成される
4. ノードを数点ドラッグして形を整える（細い突起を丸める等）
5. アクキーなら「穴を追加」、アクスタなら「台座を追加」
6. 「書き出し」→ カットライン.svg と 絵柄.pdf が出力される
```

**目標**: 初回利用者がチュートリアルなしで、この 6 ステップを 5 分以内に完了できること。

### 2.3 副次的なユースケース

- 既存の SVG カットラインを読み込んで微調整する
- 複数の絵柄を1枚のシートに並べて面付けし、まとめて入稿する（v2 以降）
- 印刷所ごとの入稿規定に合わせて数値を変える（v2 以降）

---

## 3. スコープ

### 3.1 v1.0 に含めるもの

| # | 機能 | 概要 |
|---|---|---|
| F-01 | 画像インポート | PNG / JPEG / WebP / SVG の配置、変形（移動・拡縮・回転） |
| F-02 | 背景透過 | ローカル AI モデルによる自動切り抜き |
| F-03 | ベジェパス編集 | パス描画、ノード追加・削除・移動、ハンドル操作 |
| F-04 | カットライン自動生成 | アルファ形状 → 輪郭抽出 → オフセット → ベジェ化 |
| F-05 | パーツ配置 | キーホルダー穴、アクスタ台座（差し込みスリット付き） |
| F-06 | レイヤー | レイヤーの追加・並べ替え・表示切替・ロック |
| F-07 | SVG エクスポート | カットライン（ベクター、実寸 mm） |
| F-08 | PDF エクスポート | 印刷絵柄（原寸、高解像度、透過保持） |
| F-09 | ネイティブ保存形式 | `.kkm`（作業状態の完全な保存・復元） |
| F-10 | 元に戻す／やり直す | 全操作に対する無制限の undo/redo |
| F-11 | クロスプラットフォーム配布 | Windows / macOS / Linux (omarchy = Arch + Hyprland) |

### 3.2 v1.0 に含めないもの（明示的な非スコープ）

- テキストツール（フォントの埋め込み・アウトライン化は複雑度が高い）
- グラデーション、パターン塗り、フィルタ効果
- 手動の切り抜きツール（マジックワンド、消しゴムブラシ） → **v1.5 で追加予定**。AI の結果を微修正したいケースは必ず出るため、優先度は高い
- 白版（ホワイトインク層）の自動生成 → **v2 で検討**
- 印刷所ごとの入稿プリセット → **v2 で検討**
- 面付け（複数個の並べ配置）
- 複数ドキュメントの同時編集（1ウィンドウ1ドキュメント）
- クラウド同期、共同編集

### 3.3 将来の拡張候補

| 優先度 | 機能 | 理由 |
|---|---|---|
| 高 | 手動リタッチ（消しゴム／復元ブラシ） | AI 切り抜きは髪の毛・半透明部分で必ず失敗する |
| 中 | 白版自動生成 | UV プリンタ入稿では実質必須。アルファから生成し、チョーク量（内側縮小）を指定して PDF にスポットカラーで出力 |
| 中 | 入稿プリセット | 業者ごとの塗り足し・最小線幅・レイヤー命名規則をプロファイル化 |
| 中 | 面付け | 1シートに複数個並べてコスト削減 |
| 低 | バッチ処理 | 大量の画像を同一設定で一括処理 |
| 低 | プラグイン API | ユーザー定義のパーツテンプレート |

---

## 4. アーキテクチャ

### 4.1 技術スタック

| 層 | 採用技術 | 選定理由 |
|---|---|---|
| アプリシェル | **Tauri v2** | バイナリが軽量（~10MB）。OS ネイティブの WebView を使うため Wayland/Hyprland でも素直に動く。Rust で重い処理を書ける |
| フロントエンド | **TypeScript + Vite + Solid.js** | Solid は細粒度リアクティビティで、キャンバス編集のような高頻度更新に強い。仮想 DOM の diff コストがない |
| 状態管理 | **Zustand 相当の自前ストア（Solid signals ベース）** | ドキュメントモデルは immutable な操作履歴として持つ |
| キャンバス描画 | **SVG DOM（v1）** | v1 の想定要素数は数十オーダー。SVG DOM なら実装が最も速く、ヒットテストもブラウザ任せにできる。要素数が増える v2 以降で Canvas2D + Path2D への差し替えを検討 |
| ジオメトリ演算 | **Rust: `kurbo` + `clipper2`** | ベジェ数学は kurbo、ポリゴンのオフセット／ブーリアンは Clipper2 |
| 輪郭抽出 | **Rust: 自前のマーチングスクエア + RDP 単純化 + Schneider ベジェフィッティング** | ライブラリに丸投げせず制御したい部分。VTracer(`visioncortex`) も候補 |
| AI 推論 | **Rust: `ort`（ONNX Runtime bindings）** | クロスプラットフォームで実績があり、各 OS の GPU バックエンドに繋げられる |
| PDF 生成 | **Rust: `printpdf`** | 画像埋め込み、SMask（透過）、ICC プロファイル、TrimBox/BleedBox の制御が可能 |
| 画像処理 | **Rust: `image` + `fast_image_resize`** | デコード、リサイズ、アルファ合成 |
| ビルド／配布 | **Tauri Bundler + GitHub Actions** | 各 OS のマトリクスビルド |

### 4.2 プロセス構成

```
┌─────────────────────────────────────────────────────┐
│  WebView (TypeScript / Solid.js)                    │
│                                                     │
│  ・UI シェル（ツールバー、レイヤーパネル、          │
│    プロパティインスペクタ）                         │
│  ・キャンバス（SVG レンダリング、ヒットテスト、     │
│    マウス／ペン入力ハンドリング）                   │
│  ・ドキュメントモデルの保持（single source of truth）│
│  ・undo/redo スタック                               │
│  ・軽量なベジェ演算（ドラッグ中のプレビュー用）     │
└──────────────────┬──────────────────────────────────┘
                   │ Tauri IPC (invoke / channel)
                   │ 大きなバイナリは共有一時ファイル経由
┌──────────────────┴──────────────────────────────────┐
│  Rust Core                                          │
│                                                     │
│  ・geometry:  オフセット、ブーリアン、単純化、      │
│               ベジェフィッティング                  │
│  ・trace:     アルファ → 輪郭ポリゴン               │
│  ・matting:   ONNX 推論による背景透過               │
│  ・raster:    デコード、リサイズ、色空間変換        │
│  ・export:    PDF 生成、SVG シリアライズ            │
│  ・fs:        .kkm の読み書き                       │
└─────────────────────────────────────────────────────┘
```

**責務の境界**: 「対話的な操作のフィードバック」はフロントエンドで完結させ（60fps を守る）、「時間のかかる確定的な計算」は Rust に投げる。ドラッグ中のカットラインプレビューはフロント側の近似オフセットで描き、マウスを離した時点で Rust の正確な結果に差し替える。

### 4.3 IPC の設計方針

- コマンドは全て `async`。長時間処理は Tauri の Channel で進捗イベントを流す。
- 画像バイナリを IPC の JSON に載せない。Rust 側で一時ディレクトリにファイルを書き、フロントには `asset://` の URL とハンドル ID を返す。
- キャンセル可能な処理（AI 推論など）は `AbortHandle` をトークンで管理する。

```typescript
// 呼び出し側のイメージ
const handle = await invoke<ImageHandle>('import_image', { path });

const channel = new Channel<MattingProgress>();
channel.onmessage = (p) => setProgress(p.percent);
const cutout = await invoke<ImageHandle>('remove_background', {
  handle, model: 'birefnet-lite', onProgress: channel,
});

const path = await invoke<BezierPath>('generate_cutline', {
  handle: cutout,
  offsetMm: 3.0,
  smoothing: 0.5,
  alphaThreshold: 128,
});
```

---

## 5. データモデル

### 5.1 ドキュメント

すべての座標は **mm 単位の実寸**で保持する。ピクセルは表示時にのみ登場する。これは印刷用ツールとして最も重要な設計判断であり、Inkscape が単位系で混乱を招いている点を回避する。

```typescript
interface Document {
  version: 1;
  id: string;
  meta: {
    title: string;
    created: string;   // ISO 8601
    modified: string;
  };
  /** 仕上がりサイズ。アクリル板の外形が収まる領域 */
  canvas: {
    widthMm: number;
    heightMm: number;
    /** 塗り足し（デフォルト 3mm） */
    bleedMm: number;
  };
  /** 素材の実体。ノードから参照される */
  assets: Record<AssetId, Asset>;
  layers: Layer[];
  /** アクリルグッズ固有の設定 */
  product: ProductSettings;
}

interface ProductSettings {
  kind: 'acrylic-stand' | 'acrylic-keychain' | 'custom';
  /** アクリル板の厚み。台座スリット幅の計算に使う */
  thicknessMm: number;      // デフォルト 3.0
  /** カットラインの標準オフセット量 */
  defaultOffsetMm: number;  // デフォルト 3.0
}

interface Layer {
  id: LayerId;
  name: string;
  /** レイヤーの役割。エクスポート時の振り分けに使う */
  role: 'artwork' | 'cutline' | 'parts' | 'guide';
  visible: boolean;
  locked: boolean;
  opacity: number;
  nodes: Node[];
}

type Node = ImageNode | PathNode | GroupNode;

interface NodeBase {
  id: NodeId;
  name: string;
  visible: boolean;
  locked: boolean;
  /** 親座標系への変換。[a,b,c,d,e,f] の affine */
  transform: Matrix;
}

interface ImageNode extends NodeBase {
  type: 'image';
  assetId: AssetId;
  /** 配置サイズ（mm）。元画像の DPI とは独立 */
  widthMm: number;
  heightMm: number;
  /** 背景透過の適用状態 */
  matting?: {
    model: string;
    /** 元画像に戻せるよう、加工後を別アセットとして持つ */
    resultAssetId: AssetId;
  };
}

interface PathNode extends NodeBase {
  type: 'path';
  /** 複数サブパス。穴あき形状を表現できる */
  subpaths: SubPath[];
  fill: Paint | null;
  stroke: Stroke | null;
  /** このパスがどう作られたか。再生成に必要な情報 */
  origin?: CutlineOrigin | PartOrigin;
}

interface SubPath {
  closed: boolean;
  /** ベジェのアンカー列 */
  anchors: Anchor[];
}

interface Anchor {
  /** アンカー点（mm） */
  p: Point;
  /** 入りハンドル（p からの相対、mm） */
  in: Point;
  /** 出ハンドル（p からの相対、mm） */
  out: Point;
  /** ハンドルの連動方式 */
  kind: 'corner' | 'smooth' | 'symmetric';
}

/** カットラインの生成元。パラメータ変更で再生成できる */
interface CutlineOrigin {
  type: 'cutline';
  sourceNodeId: NodeId;
  params: CutlineParams;
  /** ユーザーが手で編集したか。true なら再生成時に警告する */
  manuallyEdited: boolean;
}

interface CutlineParams {
  offsetMm: number;
  alphaThreshold: number;   // 0-255
  /** 単純化の強さ。0=忠実, 1=最大限なめらか */
  smoothing: number;
  /** これより小さい穴・島を無視する（mm²） */
  minAreaMm2: number;
  /** 内側の穴を残すか。通常のアクスタは false */
  keepHoles: boolean;
  /** 角の処理 */
  joinStyle: 'round' | 'miter';
}
```

### 5.2 ファイル形式 `.kkm`

ZIP コンテナ。中身が human-readable であることを重視する（バージョン管理・デバッグ・将来の互換性のため）。

```
document.kkm  (ZIP, 無圧縮 or Deflate)
├── mimetype                  # "application/vnd.kakomu.document" (無圧縮・先頭固定)
├── document.json             # 上記 Document を JSON 化したもの
├── assets/
│   ├── <asset-id>.png        # 元画像（無改変で保持）
│   ├── <asset-id>.cutout.png # 背景透過の結果
│   └── manifest.json         # ハッシュ、元ファイルパス、EXIF 等
└── thumbnail.png             # 256x256 のプレビュー（OS のファイラ向け）
```

**設計上の決め事**:
- 元画像は必ず無改変で保持する。非破壊編集を保証するため、ファイルサイズより復元可能性を優先する。
- `document.json` は整形して書き出す（`git diff` が読める）。
- 未知のフィールドは読み込み時に保持し、書き出し時に復元する（前方互換）。

### 5.3 undo/redo

コマンドパターン。各操作は `apply` / `revert` を持つ。

```typescript
interface Command {
  label: string;              // UI に表示（「カットラインを生成」）
  apply(doc: Document): Document;
  revert(doc: Document): Document;
  /** 連続する同種操作をまとめるか（ドラッグ中の移動など） */
  coalesceKey?: string;
}
```

ドキュメント全体のスナップショットは取らない（画像アセットを含むためメモリを食う）。差分のみを保持する。

---

## 6. 機能仕様

### 6.1 F-01 画像インポート

**入力形式**: PNG, JPEG, WebP, TIFF, SVG, HEIC (macOS のみ / 可能なら全 OS)

**動作**:
- ドラッグ&ドロップ、ファイルメニュー、クリップボード貼り付けに対応
- インポート時に画像の DPI メタデータを読み、実寸を推定して配置する。DPI 不明なら 350dpi と仮定（印刷用の標準的な解像度）
- キャンバスより大きい画像は、キャンバスに収まるよう自動でスケールして配置
- **解像度の警告**: 配置サイズに対する実効解像度が 300dpi を下回る場合、キャンバス上に非侵襲的な警告バッジを表示する。UV プリント出力で最も多いトラブルがこれであるため

**変形**:
- 選択ハンドルによる移動・拡縮（Shift で縦横比固定）・回転（Shift で 15° スナップ）
- プロパティパネルでの数値入力（X, Y, W, H, 角度、すべて mm / 度）

### 6.2 F-02 背景透過

**方式**: ローカル ONNX モデルによる自動セグメンテーション。ネットワーク接続を必要としない。

**モデル**:

| モデル | サイズ | 用途 | 備考 |
|---|---|---|---|
| `isnet-general-use` | 約 170MB | 汎用（既定） | rembg で実績のあるモデル。写真・イラストとも安定 |
| `u2netp` | 約 4.7MB | 軽量・低スペック機向け | 精度は落ちるがプレビュー用途に十分 |
| `birefnet-lite` | 約 220MB | 高精度（髪の毛・毛皮） | 推論が重い。オプション扱い |

**配布方針**: `u2netp` のみアプリに同梱し、それ以外は初回使用時にダウンロードする。インストーラのサイズを抑えつつ、オフラインでも最低限動く状態を保証するため。ダウンロード URL とチェックサムはアプリに埋め込む。

**実行バックエンド**:

| OS | 優先バックエンド | フォールバック |
|---|---|---|
| macOS | CoreML EP | CPU (Accelerate) |
| Windows | DirectML EP | CPU |
| Linux | CUDA EP（あれば） | CPU |

**処理フロー**:
```
1. 入力画像を モデル入力サイズ（1024x1024 等）にリサイズ
2. 推論 → アルファマスク（1ch, float）
3. 元解像度にアップサンプリング（bicubic）
4. エッジ処理:
   - しきい値処理は行わない（ソフトエッジを保持）
   - 「エッジを締める」オプション（デフォルト ON）:
     マスクに軽い contrast curve を適用し、半透明の縁を減らす
5. 元画像の RGB × マスク → RGBA PNG として新アセットを作成
```

**UI**:
- ワンクリックで実行。進捗バーとキャンセルボタンを表示
- 結果はビフォー／アフターのスライダーで比較できる
- 「元に戻す」で透過前の状態に戻せる（元アセットは常に保持されている）
- 調整可能なパラメータ: モデル選択、エッジの締め具合、マスクの拡張／収縮（±px）

**制約と伝え方**: 髪の毛、透明なガラス、白背景に白い服といったケースでは失敗する。失敗を検知する術はないため、「結果を必ず目視確認してください」というガイダンスを結果表示時に添える。手動修正ツールは v1.5 で追加する。

### 6.3 F-03 ベジェパス編集

Inkscape のノードツールを踏襲しつつ、機能を絞る。

**パスツール（描画）**:
- クリックでコーナーアンカー、ドラッグでスムーズアンカーを追加
- 始点クリックでパスを閉じる
- Enter / Esc で確定
- 描画中の Backspace で直前のアンカーを削除

**ノードツール（編集）**:

| 操作 | 挙動 |
|---|---|
| クリック | アンカー選択 |
| ドラッグ（矩形） | 範囲内のアンカーを複数選択 |
| アンカーをドラッグ | 移動（Shift で軸拘束、Ctrl でグリッドスナップ） |
| ハンドルをドラッグ | 曲率変更 |
| Alt + ハンドルドラッグ | 対向ハンドルを連動させない（コーナー化） |
| セグメント上をダブルクリック | アンカーを挿入 |
| Delete | アンカー削除（両隣のカーブを維持するようハンドルを再計算） |
| Ctrl + Delete | アンカー削除（カーブを維持しない） |
| S | 選択アンカーを smooth に |
| C | 選択アンカーを corner に |

**パス操作**:
- 結合、分割、方向反転
- ブーリアン演算: 和・差・積・排他（Clipper2 経由）
- パスの単純化（アンカー数を減らす）

**スナップ**:
- グリッド、他のアンカー、パスの交点、キャンバス境界
- スナップの ON/OFF は `%` キーでトグル（Inkscape 互換）

### 6.4 F-04 カットライン自動生成

このアプリの中核機能。

**入力**: アルファチャンネルを持つ ImageNode（背景透過済みが前提だが、元から透過 PNG でもよい）

**アルゴリズム**:

```
[1] アルファ二値化
    alpha >= threshold (既定 128) を前景とする二値マスク

[2] ノイズ除去
    モルフォロジー的クロージング（3x3, 1回）で 1px の穴を埋める
    連結成分ラベリング → minAreaMm2 未満の島を除去

[3] 輪郭抽出
    マーチングスクエアで前景の境界ポリゴンを取得
    外輪郭と内輪郭（穴）を区別し、keepHoles=false なら穴を破棄

[4] ポリゴン単純化
    Ramer-Douglas-Peucker、許容誤差 = smoothing に基づいて 0.05〜0.5mm

[5] オフセット
    Clipper2 の InflatePaths で offsetMm だけ外側に拡張
    joinStyle=round: 角を半径 offsetMm で丸める（アクリル加工では既定）
    joinStyle=miter: 角を尖らせる（miter limit = 2.0）

[6] 自己交差の解消
    オフセット結果を Union して、内部に生じたループを除去

[7] 細部の除去（重要）
    オフセット後に生じた「細いくびれ」を検出する:
      形状を -w だけ内側にオフセット → +w だけ外側に戻す（opening）
      w = 最小許容幅 / 2（既定 1.5mm）
    くびれが解消された箇所は、アクリル加工で折れやすい部位なので、
    この opening 結果を採用する

[8] ベジェフィッティング
    Schneider の曲線あてはめアルゴリズムで、
    ポリゴン列を三次ベジェ列に変換
    許容誤差 = 0.02mm（目視で直線化が分からないレベル）
    コーナー検出: 隣接セグメントの角度差が 60° を超える点はコーナーとして分割

[9] PathNode として cutline レイヤーに追加
```

**パラメータ UI**（サイドパネル、変更するとライブプレビュー）:

| パラメータ | 既定値 | 範囲 | 説明 |
|---|---|---|---|
| オフセット量 | 3.0mm | 0〜20mm | 絵柄から縁までの白フチ幅 |
| アルファしきい値 | 128 | 1〜254 | 半透明部分をどこから前景とみなすか |
| なめらかさ | 0.5 | 0〜1 | 大きいほどアンカーが減り丸くなる |
| 最小面積 | 4.0mm² | 0〜100 | これ未満の孤立した点を無視 |
| 穴を残す | OFF | — | ドーナツ状の抜きを残すか |
| 角の処理 | 丸 | 丸/尖り | 加工上は丸を推奨 |
| 最小幅チェック | ON | — | 折れやすい細部を自動で太らせる |

**再生成**: パラメータを変えると再生成される。ただしユーザーが手動でノードを編集した後（`manuallyEdited: true`）は「手動の変更が失われます」という確認を挟む。

**手動編集への引き継ぎ**: 生成されたカットラインは通常の PathNode なので、そのままノードツールで編集できる。生成 → 微調整、という流れを分断しない。

**検証（バリデーション）**: 生成後に自動でチェックし、問題箇所をキャンバス上にマーカー表示する。

| チェック | 条件 | 重要度 |
|---|---|---|
| 最小幅 | 形状の幅が 1.5mm 未満の箇所 | エラー |
| 最小穴径 | 内側の穴が φ2mm 未満 | エラー |
| 自己交差 | パスが自身と交差している | エラー |
| 開いたパス | 閉じていないサブパス | エラー |
| キャンバスはみ出し | 仕上がり領域を超えている | 警告 |
| 絵柄の露出 | 絵柄がカットラインからはみ出している | 警告 |
| 鋭角 | 内角 20° 未満の角 | 警告（欠けやすい） |

### 6.5 F-05 パーツ配置

パラメトリックなテンプレートとして提供する。配置後もパラメータを変更でき、その場で再生成される。

#### キーホルダー穴

```typescript
interface HolePart {
  type: 'hole';
  diameterMm: number;        // 既定 4.0（一般的なボールチェーン・カニカン用）
  /** 穴の中心から、カットラインの縁までの最小距離 */
  marginMm: number;          // 既定 3.0（強度確保のため）
  center: Point;
}
```

- 円をベジェ（4 セグメント、magic number 0.5522847498）で生成
- 配置時、カットライン形状の内側に自動でスナップし、`marginMm` を満たす位置を提案する
- `marginMm` を満たさない位置に置かれた場合はエラーマーカーを出す
- カットラインとの関係: 穴は「カットラインのサブパスとして内輪郭で追加する」方式を採る。別パスにすると、加工機側でどちらを切るか判定できない場合があるため

#### アクスタ台座

```typescript
interface StandPart {
  type: 'stand';
  /** 台座の外形 */
  shape: 'rounded-rect' | 'ellipse' | 'trapezoid';
  widthMm: number;           // 既定 60
  heightMm: number;          // 既定 20
  cornerRadiusMm: number;    // 既定 3
  /** 差し込みスリット */
  slot: {
    /** アクリル厚 + クリアランス。 product.thicknessMm + 0.2 が既定 */
    widthMm: number;
    lengthMm: number;        // 既定 25
    /** スリットの端を丸めるか（応力集中を避ける） */
    roundedEnds: boolean;    // 既定 true
  };
}
```

- 台座本体とスリットを同時に生成し、スリットは内輪郭として台座パスに含める
- 本体側（絵柄側）にも、対応する「差し込みタブ」を生成するオプション
  - タブ幅 = スリット長 - 0.4mm、タブ高 = 台座厚 + 1mm
- 台座は「絵柄と同じシートに面付けする」ため、キャンバスの空きスペースに自動配置する

**パーツはすべて `parts` ロールのレイヤーに置かれ、SVG エクスポート時にカットラインと統合される。**

### 6.6 F-06 レイヤー

初期状態で以下の 3 レイヤーが作られる。ユーザーは追加・削除できるが、`role` は変更できない（エクスポートの振り分けに使うため）。

| レイヤー名 | role | 内容 |
|---|---|---|
| 絵柄 | `artwork` | インポートした画像。PDF エクスポート対象 |
| カットライン | `cutline` | 生成／編集されたカットライン。SVG エクスポート対象 |
| パーツ | `parts` | 穴、台座。SVG エクスポート対象 |

`guide` ロールのレイヤーは、参照用の下絵などに使い、どちらのエクスポートにも含まれない。

### 6.7 F-10 元に戻す／やり直す

- 無制限（メモリ上限まで）。上限に達したら古い履歴から破棄する
- ショートカット: `Ctrl/Cmd+Z`, `Ctrl/Cmd+Shift+Z`
- 履歴パネルで任意の時点にジャンプできる
- AI 背景透過も 1 コマンドとして履歴に載る（結果アセットを保持しているので即座に戻せる）

---

## 7. エクスポート

### 7.1 F-07 SVG エクスポート（カットライン）

**目的**: レーザーカッター／カッティングプロッタ／アクリル加工業者への入稿。

**出力仕様**:

```xml
<svg xmlns="http://www.w3.org/2000/svg"
     width="80mm" height="120mm"
     viewBox="0 0 80 120">
  <!-- ユーザー座標系 1 単位 = 1mm。加工機側で確実に実寸になる -->
  <g id="cutline" inkscape:label="CutContour" inkscape:groupmode="layer">
    <path d="M ..." fill="none" stroke="#FF00FF" stroke-width="0.1"/>
  </g>
</svg>
```

**設計上の決め事**:
- `width`/`height` に物理単位（mm）を明記し、`viewBox` を mm 値と 1:1 にする。これにより、どのソフトで開いても実寸が保たれる
- 線は `fill="none"`, `stroke-width="0.1mm"` のヘアライン。カットラインの標準的な表現
- 色は マゼンタ `#FF00FF`（CutContour の慣例色）。設定で変更可能
- レイヤー名は `CutContour` を既定とする。Inkscape 互換の `inkscape:label` 属性も併記し、Illustrator でも Inkscape でもレイヤー名として認識されるようにする
- 変換（transform）はすべてベイクし、パスデータを絶対座標にする。加工機のソフトが transform を解釈できないケースがあるため
- 円弧は使わず三次ベジェ（C コマンド）のみで出力する。互換性が最も高い

**オプション**:
- 絵柄をラスタとして埋め込む（プレビュー用、`<image>` として base64）
- パーツを含める／含めない
- 線の色・太さ

### 7.2 F-08 PDF エクスポート（印刷絵柄）

**目的**: UV プリンタの RIP ソフトへの入稿。

**出力仕様**:

| 項目 | 値 |
|---|---|
| PDF バージョン | 1.7 |
| ページサイズ | キャンバスサイズ + 塗り足し |
| MediaBox | 仕上がり + 塗り足し |
| TrimBox | 仕上がりサイズ |
| BleedBox | MediaBox と同じ |
| 画像の解像度 | 既定 600dpi（設定可: 300 / 600 / 1200 / 原寸維持） |
| 画像の圧縮 | 可逆（FlateDecode）を既定。JPEG(DCTDecode) は選択制 |
| 透過 | SMask としてアルファチャンネルを保持 |
| 色空間 | 既定 RGB（sRGB ICC 埋め込み）。CMYK 変換はオプション |

**色空間についての判断**: UV プリンタの RIP は多くの場合 RGB 入稿を推奨する（プリンタ側のプロファイルで変換した方が色域を広く使えるため）。したがって **既定は sRGB とし、ICC プロファイルを埋め込む**。CMYK 変換が必要な業者向けに、`lcms2` を使った変換をオプションで提供する（プロファイルは Japan Color 2011 Coated 等をユーザーが指定）。

**構成**:
```
Page 1
├── (optional) カットライン  … スポットカラー "CutContour" のパスとして
└── 絵柄画像                  … XObject + SMask
```

カットラインを PDF に含めるかは選択制。「絵柄 PDF とカットライン SVG を別々に入稿」が最も一般的だが、「1つの PDF に両方」を求める業者もあるため両対応する。

**書き出しダイアログ**:
- 出力先とファイル名（既定: `<ドキュメント名>_print.pdf` / `<ドキュメント名>_cut.svg`）
- 「両方まとめて書き出す」ボタンを主導線に置く。ユーザーが片方を忘れるのを防ぐ
- 書き出し前にバリデーション（6.4 のチェック）を走らせ、エラーがあれば警告する

---

## 8. UI 設計

### 8.1 画面構成

```
┌────────────────────────────────────────────────────────────┐
│ メニューバー（Win/Linux は自前描画、macOS はネイティブ）    │
├──────┬──────────────────────────────────────┬──────────────┤
│      │                                      │              │
│ ツール│           キャンバス                 │ プロパティ   │
│ バー  │                                      │ パネル       │
│      │    ┌────────────────┐                │              │
│ [選択]│    │                │  ← 仕上がり枠  │ ・選択中の   │
│ [ノード]   │   絵柄         │     (実線)     │   要素の情報 │
│ [パス]│    │                │                │ ・カットライン│
│ [矩形]│    └────────────────┘  ← 塗り足し    │   パラメータ │
│ [円] │        ....................  (破線)   │ ・バリデーション│
│ [穴] │                                      │   結果       │
│ [台座]│                                      │              │
│      │                                      ├──────────────┤
│      │                                      │ レイヤー     │
│      │                                      │ ・絵柄       │
│      │                                      │ ・カットライン│
│      │                                      │ ・パーツ     │
├──────┴──────────────────────────────────────┴──────────────┤
│ ステータスバー: 座標(mm) / ズーム / 実効解像度 / 警告件数    │
└────────────────────────────────────────────────────────────┘
```

### 8.2 設計方針

**「次にやること」を常に提示する**: Inkscape の最大の障壁は「何から始めればいいか分からない」こと。キャンバスが空のときは大きなドロップゾーンと「画像を開く」ボタンだけを表示し、画像が入ったら「背景を消す」を、透過が終わったら「カットラインを作る」を、それぞれ目立つ位置に提示する。ウィザードではなく、あくまで通常の UI 上でのハイライトとして行う（上級者の邪魔をしない）。

**破壊的操作を作らない**: 背景透過も、カットライン生成も、元データを消さない。いつでも戻れることが分かっていれば、ユーザーは試行錯誤できる。

**単位は常に mm で表示する**: ピクセルを UI に一切出さない。例外は「実効解像度（dpi）」の表示のみ。

**キーボードショートカットは Inkscape 互換を基本とする**: `S`=選択, `N`=ノード, `B`=ベジェ, `+`/`-`=ズーム, `3`=描画全体表示, `5`=ページ全体表示。Inkscape からの移行者の学習コストを下げる。

### 8.3 テーマ

ライト／ダーク／システム追従。omarchy はダークテーマ主体の環境なので、ダークを一級市民として扱う（後付けにしない）。

### 8.4 国際化

v1 は日本語と英語。文字列は全て `i18n` リソースに外出しする。アクリルグッズ制作は日本の同人文化との結びつきが強いため、**日本語を第一言語として設計し、英語を翻訳とする**（逆にしない）。

---

## 9. クロスプラットフォーム対応

### 9.1 対応プラットフォーム

| OS | 最低バージョン | 配布形式 | 優先度 |
|---|---|---|---|
| Windows | 10 (1809) / 11 | MSI, NSIS インストーラ | 高 |
| macOS | 12 Monterey 以降 | .dmg（Universal: arm64 + x86_64） | 高 |
| Linux (汎用) | glibc 2.31+ | AppImage | 高 |
| Linux (Arch / omarchy) | — | AUR パッケージ (`kakomu-bin`) | 高 |
| Linux (Debian/Ubuntu) | — | .deb | 中 |
| Linux (Fedora) | — | .rpm | 低 |

### 9.2 omarchy / Wayland 固有の対応

omarchy は Arch Linux + Hyprland (Wayland) 構成であり、Tauri (WebKitGTK) には既知の落とし穴がいくつかある。

| 課題 | 対応 |
|---|---|
| WebKitGTK の DMABUF レンダラが Nvidia ドライバや一部の Wayland コンポジタで描画崩れを起こす | 起動時に環境を検出し、必要なら `WEBKIT_DISABLE_DMABUF_RENDERER=1` を設定。ラッパースクリプトで対応し、手動上書き用の環境変数も用意する |
| 日本語 IME（fcitx5 / ibus） | `GTK_IM_MODULE` / `QT_IM_MODULE` / `XMODIFIERS` の設定を検出し、未設定なら起動時に警告する。テキスト入力は v1 ではファイル名等に限られるが、v2 のテキストツールで本格的に必要になる |
| ファイルダイアログ | `xdg-desktop-portal` 経由（Tauri v2 の dialog プラグインが対応済み）。Hyprland では `xdg-desktop-portal-hyprland` + `xdg-desktop-portal-gtk` の両方が必要な点をドキュメントに明記 |
| ウィンドウ装飾 | Hyprland はサーバサイド装飾を持たない。クライアントサイド装飾（CSD）を自前描画する |
| HiDPI / 分数スケーリング | Wayland の `wp-fractional-scale-v1` に依存。WebView 側の `devicePixelRatio` を信頼し、キャンバスのレンダリング解像度を合わせる |
| グローバルショートカット | Wayland ではアプリ単位でしか取れない。グローバルショートカットは使わない設計にする |

**CI での検証**: GitHub Actions に Arch Linux コンテナのジョブを置き、ビルドと最小限の起動確認（`--version` 相当のヘッドレス実行）を行う。GUI の実動作確認は手動テストとする。

### 9.3 パッケージング

- **コード署名**: Windows は EV 証明書、macOS は Developer ID + notarization。未署名だと初回起動でブロックされ、ユーザーが離脱する。個人開発の場合、初期は「署名なしで配布し、回避手順をドキュメント化」→ ユーザーが増えたら署名を導入、という段階を踏む
- **自動更新**: Tauri の updater プラグイン。AppImage と AUR は自動更新を無効化する（パッケージマネージャの管轄を侵さないため）
- **ONNX モデル**: バイナリに同梱するのは `u2netp`（4.7MB）のみ。他はアプリのデータディレクトリにダウンロードする

---

## 10. 非機能要件

### 10.1 パフォーマンス目標

| 操作 | 目標 | 上限 |
|---|---|---|
| アプリ起動（コールドスタート） | 1.0s | 2.0s |
| 画像インポート（4000x4000px） | 0.5s | 1.5s |
| 背景透過（`isnet`, CPU, 4000x4000px） | 8s | 20s |
| 背景透過（`isnet`, GPU） | 2s | 5s |
| カットライン生成（4000x4000px） | 0.8s | 2.0s |
| ノードドラッグ中のフレームレート | 60fps | 30fps |
| ズーム／パン | 60fps | 30fps |
| PDF エクスポート（600dpi, A5） | 3s | 10s |
| `.kkm` 保存（画像 20MB） | 1s | 3s |

**測定環境の基準**: MacBook Air M1 (8GB) を「標準的な下限」とする。

### 10.2 リソース

- アイドル時メモリ: 300MB 以下
- 画像 1 枚（4000x4000）編集時: 1.2GB 以下
- インストールサイズ: 120MB 以下（同梱モデル込み）

### 10.3 信頼性

- **クラッシュ耐性**: 30 秒ごとに自動保存（`.kkm.autosave`）。次回起動時に復元を提案する
- **エラー処理**: Rust 側の panic を捕捉し、UI にエラーダイアログとして表示する。プロセス全体を落とさない
- **大きい画像への防御**: 16000x16000px を超える画像はインポート時に警告し、縮小を提案する

### 10.4 プライバシー

- 画像データを外部に送信しない。AI 推論はすべてローカル
- テレメトリはデフォルト無効。有効化する場合もオプトインとし、画像データは一切含めない
- ネットワークアクセスは以下に限定する: (1) モデルの初回ダウンロード, (2) アップデートチェック。どちらも設定で無効化できる

---

## 11. リポジトリ構成

```
kakomu/
├── SPEC.md
├── README.md
├── CLAUDE.md                      # AI 開発支援向けのプロジェクト規約
├── package.json
├── pnpm-workspace.yaml
│
├── src/                           # フロントエンド (TypeScript / Solid.js)
│   ├── main.tsx
│   ├── app/
│   │   ├── App.tsx
│   │   ├── shortcuts.ts
│   │   └── i18n/
│   │       ├── ja.json
│   │       └── en.json
│   ├── document/                  # ドキュメントモデル
│   │   ├── types.ts
│   │   ├── store.ts
│   │   ├── commands/              # undo/redo コマンド群
│   │   └── serialize.ts
│   ├── canvas/                    # キャンバス描画・入力
│   │   ├── Canvas.tsx
│   │   ├── renderer/
│   │   ├── hittest.ts
│   │   ├── snapping.ts
│   │   └── tools/                 # 各ツールの状態機械
│   │       ├── SelectTool.ts
│   │       ├── NodeTool.ts
│   │       ├── PathTool.ts
│   │       └── PartTool.ts
│   ├── panels/                    # レイヤー、プロパティ、履歴
│   ├── geometry/                  # フロント側の軽量ベジェ演算
│   └── ipc/                       # Rust コマンドの型付きラッパ
│
├── src-tauri/                     # Rust バックエンド
│   ├── Cargo.toml
│   ├── tauri.conf.json
│   ├── src/
│   │   ├── main.rs
│   │   ├── commands/              # #[tauri::command] の定義
│   │   ├── geometry/              # オフセット、ブーリアン、フィッティング
│   │   │   ├── offset.rs
│   │   │   ├── simplify.rs
│   │   │   ├── fit.rs
│   │   │   └── validate.rs
│   │   ├── trace/                 # アルファ → 輪郭
│   │   ├── matting/               # ONNX 推論
│   │   │   ├── model.rs
│   │   │   └── download.rs
│   │   ├── raster/
│   │   ├── export/
│   │   │   ├── svg.rs
│   │   │   └── pdf.rs
│   │   └── kkm/                   # ファイル形式
│   └── models/                    # 同梱 ONNX モデル
│
├── crates/                        # 独立させたい Rust クレート
│   └── kakomu-geometry/           # 単体でテスト・ベンチできるようにする
│
├── tests/
│   ├── e2e/                       # WebDriver ベース
│   └── fixtures/                  # テスト用画像・期待される SVG
│
└── .github/workflows/
    ├── ci.yml                     # lint, test, build (全 OS)
    └── release.yml                # タグからのリリースビルド
```

---

## 12. 開発ロードマップ

### M0: 基盤（2週間）

- Tauri v2 プロジェクトの初期化、3 OS でのビルドが通る状態
- ドキュメントモデルの型定義と store
- キャンバスの基本描画（SVG レンダラ）、ズーム・パン
- 選択ツール、画像の配置と変形
- **完了条件**: 画像を開いて動かして拡大縮小できる

### M1: エクスポート（2週間）

- SVG エクスポート
- PDF エクスポート（RGB, SMask 透過）
- `.kkm` の保存・読み込み
- undo/redo の基盤
- **完了条件**: 画像を配置して PDF と SVG に書き出せる（カットラインは手描き）

### M2: ベジェ編集（3週間）

- パスツール（描画）
- ノードツール（編集）
- スナップ
- ブーリアン演算
- **完了条件**: 画像の周りを手動でパスで囲み、SVG に書き出せる — つまり「Inkscape の代替」として最低限成立する

### M3: 背景透過（2週間）

- ONNX Runtime の統合、モデルのダウンロード管理
- 推論パイプラインと進捗表示
- ビフォー／アフター比較 UI
- 各 OS の GPU バックエンド対応
- **完了条件**: ワンクリックで背景が消える

### M4: カットライン自動生成（3週間）

- 輪郭抽出 → オフセット → ベジェフィッティングのパイプライン
- パラメータ UI とライブプレビュー
- バリデーション（最小幅、自己交差など）
- **完了条件**: ゴールデンパス（2.2 節）が通しで動く

### M5: パーツとポリッシュ（2週間）

- キーホルダー穴、アクスタ台座
- 解像度警告、エラーマーカー
- 日本語／英語の i18n
- オンボーディングの導線
- **完了条件**: v1.0 として配布できる

### M6: 配布（1週間）

- 各 OS のパッケージング、AUR パッケージ
- 自動更新
- ドキュメントサイト
- **完了条件**: 3 OS でインストールして使える

**合計見積もり: 約 15 週間**（1人でフルタイム相当の場合）

### v1.5 以降

- 手動リタッチツール（消しゴム／復元ブラシ）— AI 切り抜きの失敗を補うため優先度が高い
- 白版（ホワイトインク層）の自動生成
- 印刷所ごとの入稿プリセット
- 面付け

---

## 13. テスト戦略

| 層 | 対象 | 手段 |
|---|---|---|
| 単体（Rust） | ジオメトリ演算（オフセット、フィッティング、バリデーション） | `cargo test`。既知の入力形状に対する期待出力を固定 |
| プロパティベース（Rust） | オフセット結果の不変条件（元形状を必ず内包する、自己交差しない等） | `proptest` |
| ベンチマーク（Rust） | カットライン生成、PDF 生成 | `criterion`。性能目標（10.1 節）をリグレッションテストとする |
| 単体（TS） | ドキュメントモデル、コマンド、シリアライズ | `vitest` |
| ゴールデンファイル | SVG / PDF の出力 | 既知の `.kkm` から書き出し、期待ファイルと比較（PDF は決定的生成にするため、タイムスタンプと ID を固定できるようにする） |
| E2E | ゴールデンパス | `tauri-driver` + WebDriver。3 OS の CI で実行 |
| 手動 | omarchy 実機での描画・IME・ファイルダイアログ | リリース前チェックリスト |

**テストフィクスチャ**: 意図的に難しいケースを揃える。
- 髪の毛が細かいキャラクターイラスト
- 背景が白い白い服の人物写真
- 細い突起（尻尾、リボン）を持つ形状
- 中央に穴の開いたドーナツ形状
- 半透明のグラデーションを含む PNG
- 巨大画像（8000x8000）

---

## 14. リスクと未決事項

### 14.1 技術的リスク

| リスク | 影響 | 対策 |
|---|---|---|
| ベジェフィッティングの品質が業務に耐えない | カットラインが「なんとなく違う」形になる | M4 の早い段階でプロトタイプを作り、実際の入稿データと比較する。Schneider が不十分なら、曲率連続性を考慮した手法に切り替える |
| ONNX Runtime のクロスプラットフォームビルドが煩雑 | ビルドが不安定になる | M3 の最初に 3 OS でのビルドを通す。困難なら `tract`（pure Rust の推論エンジン）へのフォールバックを検討 |
| SVG DOM レンダリングが要素数増で遅くなる | 大きなドキュメントで操作がもたつく | v1 の想定要素数では問題にならない見込み。10.1 の目標を CI のベンチで監視し、超えたら Canvas2D に切り替える |
| WebKitGTK の Wayland 周りの不安定さ | Linux で使い物にならない | M0 の時点で omarchy 実機での動作確認を行う。致命的なら該当環境で X11 バックエンドにフォールバックする |
| PDF の色再現が業者の期待と合わない | 刷り上がりの色が違う | 既定を sRGB + ICC 埋め込みとし、根拠をドキュメントに明記。ユーザーが CMYK 変換を選べるようにする |

### 14.2 未決事項

| # | 事項 | 決める時期 |
|---|---|---|
| Q-01 | カットラインの色の既定値をマゼンタ `#FF00FF` とするか。業者によって指定が異なる可能性がある | M1 まで。実際の入稿規定を数社分調べる |
| Q-02 | キーホルダー穴を「カットラインの内輪郭」として持つか「別パス」として持つか | M5 まで。加工業者の入稿規定を確認する |
| Q-03 | 同梱モデルを `u2netp` とするか、初回起動時に `isnet` をダウンロードさせるか。インストールサイズと初回体験のトレードオフ | M3 まで |
| Q-04 | macOS のコード署名を初期から行うか（Apple Developer Program 年額 $99） | M6 まで |
| ~~Q-05~~ | ~~ライセンス~~ → **MIT に決定**。同梱・利用するモデルのライセンス（isnet は Apache-2.0）とも整合する | 決定済み |
| Q-06 | フロントエンドのフレームワークを Solid.js とするか React とするか。エコシステムの厚さを取るなら React | M0 の実装開始前 |

---

## 15. 参考にする既存実装

| プロジェクト | 参考にする点 |
|---|---|
| Inkscape | ノードツールの操作体系、ショートカット、パス演算の挙動 |
| Figma / Penpot | 選択・変形ハンドルの UX、プロパティパネルの設計 |
| rembg | 背景透過のモデル選定と前処理・後処理 |
| VTracer / potrace | ラスタからベクタへの変換手法 |
| Clipper2 | ポリゴンオフセットとブーリアン演算 |
| LightBurn | レーザー加工向けの UI。実寸表示と検証機能の見せ方 |

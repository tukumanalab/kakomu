# kakomu — 仕様書 (SPEC)

> 青学つくまなラボのための、アクリルスタンド／アクリルキーホルダー制作ツール。
> 好きな絵や写真を置き、それを囲む線をつくり、レーザー加工機と UV プリンターに渡す。

- **バージョン**: 0.2 (draft)
- **最終更新**: 2026-09-09
- **ステータス**: プランニング（実装未着手）
- **設置先**: [青学つくまなラボ](https://sites.google.com/view/tukumanalab/)（青山学院大学 青山キャンパス 10号館 地下1階）

---

## 1. 概要

### 1.1 何を作るか

Inkscape の「画像を置いて、その周囲を曲線で囲み、ベクターとして書き出す」というワークフローだけを切り出し、アクリルグッズ制作に必要な機能を足したデスクトップアプリケーション。

汎用ドローイングツールではなく、**「1 枚の絵から 1 個のアクリルグッズを作る」ための専用ツール**として設計する。Inkscape の全機能を再実装することは目的としない。

### 1.2 どこで使われるか

[青学つくまなラボ](https://sites.google.com/view/tukumanalab/)。この施設の性質が、ツールの設計をほぼ決めています。

| 施設の性質 | ツールへの帰結 |
|---|---|
| 世代の壁を取り払う（初等部〜教職員） | **同じ 1 つの画面が、小学校低学年にも大学院生にも通じなければならない**。読みやすさは機能であり、後付けのオプションではない |
| 創ることで学ぶ | エラーは門番ではなく**教材**。「この細さだと折れます」は、なぜ 1.5mm が必要かを伝える機会 |
| 予約不要のドロップイン | 起動から完成まで**その日のうちに終わる**こと。アカウント作成・チュートリアル・初期設定を挟まない |
| 加工機が同じ部屋にある | 外部業者への「入稿」ではなく、**目の前の機械に渡す**。作って、切って、持って帰るまでが 1 回の来室に収まる |

### 1.3 名前の由来

「囲む」— 絵を囲む線をつくることが、このツールの中心的な仕事であるため。

### 1.4 用語について

このリポジトリでは **「入稿」という言葉を使いません**。入稿は外部の印刷業者にデータを渡す行為を指す言葉で、加工機が同じ部屋にあるファブスペースの体験とずれるためです。代わりに次を使います。

| 使わない | 使う |
|---|---|
| 入稿データ | 加工データ／切るデータ・印刷するデータ |
| 入稿する | 加工機に渡す／出力する |
| 入稿規定 | 加工機の設定 |
| 業者 | ラボ／加工機 |

UI で実際にユーザーに見せる言葉は、さらに噛み砕きます（[3. 言葉づかいと読みやすさ](#3-言葉づかいと読みやすさ)）。

### 1.5 コアとなる価値

1. **背景を消すところから、切る線をつくるところまでが 1 本の導線に載っている** — 現在ラボでは Inkscape を使っています。Inkscape は強力な汎用ドローツールですが、この作業をするには「ビットマップのトレース」「アウトセット」「ノード編集」を順に使う必要があり、設定項目も専門用語も多い。低学年の子に渡せる導線ではありません。kakomu は Inkscape を置き換えるのではなく、**この 1 つのワークフローだけを、この場所の全員が使える形に作り直します**。
2. **アクリル加工の寸法知識が組み込まれている** — ふちの太さ 3mm、キーホルダーの穴 φ4mm、台座のスリット 3.2mm。誰も調べ直さなくてよい。
3. **切れない形を作らせない** — 細すぎる突起、小さすぎる穴を自動で検出し、なぜダメかを説明する。失敗した板は戻ってこないので、切る前に気づけることに価値がある。
4. **読める** — ひらがなだけの表示に切り替えられる。

---

## 2. 利用者とつかわれ方

### 2.1 利用者

| 利用者 | 状況 | 設計への要求 |
|---|---|---|
| **初等部の子（小 1〜6）** | スタッフの短い声かけはあるが、基本は自分で操作する。低学年は漢字がほとんど読めない | ひらがなモード。ボタンが少ない。押せば必ず何か起きる。取り返しがつく |
| **中等部・高等部の生徒** | 部活動、自由研究、文化祭のグッズ | 漢字は読めるが専門用語はまだ知らない。行為の言葉で書く |
| **大学生** | 研究、サークルのグッズ、制作課題 | 標準的な UI。数値を直接入力したい人もいる |
| **教職員** | 教材づくり、ワークショップの準備 | 同上。加えて「もう一度同じ設定で作る」再現性 |
| **ラボスタッフ** | 同時に複数人を見ている。1 人に長く付けない | ユーザーが自分で詰まりを抜けられること。エラーが自己説明的であること |

**「初心者向けモード」を別に作るのではなく、1 つの UI の読みやすさを切り替える**方針を採ります。世代で画面を分けることは、「世代の壁を取り払う」という施設の掲げ方に反します。

### 2.2 中心的なつかわれかた（ゴールデンパス）

```
1. 絵や写真を入れる
2. 「うしろを消す」    → 背景が自動で消える
3. 「囲む」            → 絵の 3mm 外側に、切るための線ができる
4. 線のかたちを整える   → 点をドラッグする
5. 部品をつける        → アクキーなら穴、アクスタなら台座
6. 「かきだす」        → 切るデータ(SVG) と 印刷するデータ(PDF) が出る
```

### 2.3 成功条件

> **初等部の子が、スタッフの短い声かけだけで、1 回の来室（13:00〜18:00 のうち）にアクリルキーホルダーを 1 つ完成させられること。**

これが唯一の成功条件です。上級者向けの機能は、この条件を壊さない限りにおいて足します。

### 2.4 副次的なつかわれかた

- 大学生が既存の SVG を読み込んで微調整する
- 教職員がワークショップの見本を作る
- 複数の絵を 1 枚のアクリル板に並べて、材料を無駄なく使う（v2 以降）

---

## 3. 言葉づかいと読みやすさ

このツールでは、**言葉づかいは機能要件です**。実装の後で翻訳者に渡すものではなく、最初から設計に組み込みます。

> **現在の状態（2026-09-10）**: 機能の実装を優先するため、**切り替えは UI から外し、漢字モードだけで動かしています**。
> ひらがなの文言（`ja-hira.json`）と仕組みは残してあり、`READING_SWITCH_ENABLED` を `true` にすれば戻ります。
> CI もひらがなの網羅を要求しない設定に変えました（要求すると、機能を足すたびにひらがなも書かねばならず実装が止まるため）。
> 下の設計そのものは変えていません。M5 で戻します。

### 3.1 2 つの表示レベル

| レベル | 対象 | 例（ボタンのラベル） |
|---|---|---|
| `ja` 通常 | 小学校高学年くらいから上 | 背景を消す |
| `ja-hira` ひらがな | 小学校低学年 | うしろを けす |

切り替えは**いつでも 1 クリック**で、画面右上に常設します。設定画面の奥に隠しません。選択は次回起動時も保持します。

ふりがなモードは作りません。ルビは行間を大きく崩し、レイアウトが 2 段階で変わることになります。**読めないなら、ふりがなを足すのではなく言葉ごと変える**方が確実で、実装も単純です。

### 3.2 ひらがなは、機械変換ではなく書き下ろす

これは実装上の最も重要な判断です。

読み仮名への変換では通じません。「背景」を「はいけい」にしても、低学年には意味が伝わらない。**言い換えが必要**なので、独立したリソースとして人間が書きます。自動生成はしません。

```json
{ "bg.remove": { "ja": "背景を消す", "hira": "うしろを けす" } }
```

ひらがなモードで変わるのは、次の 5 つだけです。**画面の構造・ボタンの位置・機能は一切変わりません。**

| 変わるもの | 内容 |
|---|---|
| 文言 | 言い換えた別のリソースに差し替える |
| 書体 | 丸ゴシック系の UD フォントに切り替える |
| 文字サイズ | 基準を 1.15 倍にする |
| 行間 | 2.0 に広げる |
| 数値の単位 | 「3mm」→「3ミリ」 |

### 3.3 ひらがなモードの書き方の規則

1. **分かち書きにする。** 小学校低学年の教科書に合わせ、文節をスペースで区切る。「きるせんを つくる」
2. **1 文を 20 文字以内にする。**
3. **カタカナ語は、意味が伝わるものだけ残す。**「アクリル」「データ」は残す。「オフセット」「レイヤー」は残さない。
4. **数値の単位はカタカナで書く。**「3mm」→「3ミリ」
5. **否定形を避ける。**「保存されていません」→「まだ ほぞんして いません」

### 3.4 用語の対応表

UI に出す言葉です。仕様書やコードの中では従来の専門用語を使い続けます（開発者の共通語彙を壊さないため）。

**通常モードでひらがなを使わない。** 通常モードは小学校高学年くらいから上が対象なので、
普通の日本語（漢字かな交じり）で書きます。「もどす」ではなく「元に戻す」。
ひらがなにするのはひらがなモードの仕事であり、両方をひらがなにすると、
どちらのモードでも中途半端になります。避けるのは**専門用語**であって、漢字ではありません。

| 内部の用語 | UI 通常 | UI ひらがな |
|---|---|---|
| インポート | 絵を入れる | えを いれる |
| 背景透過 / マッティング | 背景を消す | うしろを けす |
| カットライン | 切る線 | きる せん |
| オフセット | ふちの太さ | ふちの ふとさ |
| ベジェ曲線 | なめらかな線 | なめらかな せん |
| ノード / アンカー | 点 | てん |
| ハンドル | 引っぱるところ | ひっぱる ところ |
| アルファチャンネル | 透明なところ | すきとおって いる ところ |
| レイヤー | レイヤー | かさね |
| 塗り足し | ふちの余白 | ふちの よゆう |
| 台座 | 台座 | たてる ぶひん |
| キーホルダー穴 | 穴 | あな |
| バリデーション | チェック | ちゃんと きれるか しらべる |
| 実効解像度 | 絵の細かさ | えの こまかさ |
| エクスポート | 書き出す | ほぞんする |
| undo | 元に戻す | もどす |
| 加工データ | 切るデータ / 印刷するデータ | きる データ / いんさつする データ |

### 3.5 書体

読みやすさは書体で大きく変わります。表示レベルに応じて切り替えます。

| レベル | 書体 | 理由 |
|---|---|---|
| `ja` | Zen Kaku Gothic New / OS 標準ゴシック | 標準的な密度 |
| `ja-hira` | UD デジタル教科書体／ Zen Maru Gothic | 字形が教科書に近く、低学年が学習中の形と一致する |

Windows には UD デジタル教科書体と BIZ UDGothic が標準で入っています。macOS と Linux にはないため、**BIZ UDGothic（OFL）と Zen Maru Gothic（OFL）をアプリに同梱**し、OS 差で読みやすさが変わらないようにします。

ひらがなモードでは併せて、**基準文字サイズを 1.15 倍、行間を 2.0 に広げます**。

### 3.6 英語

v1 は日本語（2 レベル）と英語。文字列はすべて i18n リソースに外出しします。留学生・国際交流の利用があるため英語は入れますが、**日本語を第一言語として設計し、英語を翻訳とします**（逆にしない）。

---

## 4. スコープ

### 4.1 v1.0 に含めるもの

| # | 機能 | 概要 |
|---|---|---|
| F-01 | 画像を入れる | PNG / JPEG / WebP / TIFF / SVG の配置、移動・拡縮・回転 |
| F-02 | 背景を消す | ローカル AI モデルによる自動切り抜き |
| F-03 | 線を編集する | パス描画、点の追加・削除・移動、ハンドル操作 |
| F-04 | 切る線を自動生成する | アルファ形状 → 輪郭抽出 → オフセット → ベジェ化 |
| F-05 | 部品をつける | キーホルダーの穴、アクスタの台座（差し込みスリット付き） |
| F-06 | かさね（レイヤー） | 役割つき 3 層。追加・並べ替え・表示切替・ロック |
| F-07 | 切るデータを書き出す | SVG（ベクター、実寸 mm）→ レーザー加工機へ |
| F-08 | 印刷するデータを書き出す | PDF（原寸、高解像度、透過保持）→ UV プリンターへ |
| F-09 | 保存形式 | `.kkm`（作業状態の完全な保存・復元） |
| F-10 | もどす／やりなおす | 全操作に対する無制限の undo/redo |
| ~~F-11~~ | ~~読みやすさ切替~~ | **当面は漢字モードのみ。**[3](#3-言葉づかいと読みやすさ) の設計と ja-hira.json は残したまま、切り替えを UI から外している。機能を作りきることを優先するため。戻すのは `READING_SWITCH_ENABLED` を true にするだけ |
| F-12 | **共用 PC モード** | アカウント不要。終了時にデータを持ち帰る／消す導線 |
| F-13 | 配布 | Windows / macOS / Linux（omarchy を含む） |

### 4.2 v1.0 に含めないもの

- テキストツール（フォントの埋め込み・アウトライン化は複雑度が高い）
- グラデーション、パターン塗り、フィルタ効果
- 手動の切り抜きツール（マジックワンド、消しゴムブラシ） → **v1.5 で追加予定**。優先度は高い
- 3D プリンター向けの STL 書き出し
- 面付け（複数個の並べ配置）
- 複数ドキュメントの同時編集（1 ウィンドウ 1 ドキュメント）
- クラウド同期、共同編集

### 4.3 将来の拡張候補

| 優先度 | 機能 | 理由 |
|---|---|---|
| 高 | 手動リタッチ（消しゴム／復元ブラシ） | AI 切り抜きは髪の毛・半透明部分で必ず失敗する |
| 高 | ワークショップモード | 講師の画面を見ながら全員が同じ手順をなぞる。進行を揃える補助。つくまなラボはワークショップを開催している |
| 中 | 面付け | 1 枚のアクリル板に複数個並べ、材料を無駄にしない |
| 低 | 白版の自前生成 | VersaWorks が自動生成するので普段は不要。白を敷く範囲を絵柄ごとに変えたくなったときだけ（[8.3](#83-白版ホワイトインク層は作らない)） |
| 中 | 加工機プロファイル | ラボの機種ごとに線幅・レイヤー名・色を保存 |
| 低 | 作例ギャラリー | ラボの過去作品をテンプレートとして開ける |
| 低 | プラグイン API | ユーザー定義の部品テンプレート |

---

## 5. アーキテクチャ

### 5.1 技術スタック

| 層 | 採用技術 | 選定理由 |
|---|---|---|
| アプリシェル | **Tauri v2** | バイナリが軽量（~10MB）。OS ネイティブの WebView を使うため Wayland/Hyprland でも素直に動く。Rust で重い処理を書ける |
| フロントエンド | **TypeScript + Vite + Solid.js** | 細粒度リアクティビティで、キャンバス編集のような高頻度更新に強い |
| キャンバス描画 | **SVG DOM（v1）** | v1 の想定要素数は数十オーダー。実装が速く、ヒットテストをブラウザに任せられる |
| ジオメトリ演算 | **Rust: `kurbo` + `clipper2`** | ベジェ数学は kurbo、ポリゴンのオフセット／ブーリアンは Clipper2 |
| 輪郭抽出 | **Rust: マーチングスクエア + RDP + Schneider** | ライブラリに丸投げせず制御する。VTracer(`visioncortex`) も候補 |
| AI 推論 | **Rust: `ort`（ONNX Runtime bindings）** | クロスプラットフォームで実績があり、各 OS の GPU バックエンドに繋げられる |
| PDF 生成 | **Rust: `printpdf`** | 画像埋め込み、SMask（透過）、ICC、TrimBox の制御が可能 |
| 画像処理 | **Rust: `image` + `fast_image_resize`** | デコード、リサイズ、アルファ合成 |
| 配布 | **Tauri Bundler + GitHub Actions** | 各 OS のマトリクスビルド |

### 5.2 プロセス構成

```
┌─────────────────────────────────────────────────────┐
│  WebView (TypeScript / Solid.js)                    │
│                                                     │
│  ・UI シェル（ツールバー、かさねパネル、            │
│    プロパティ、読みやすさ切替）                     │
│  ・キャンバス（SVG レンダリング、ヒットテスト、     │
│    マウス／ペン／タッチ入力）                       │
│  ・ドキュメントモデルの保持                         │
│  ・undo/redo スタック                               │
│  ・i18n（2 レベル × 2 言語）                        │
│  ・軽量なベジェ演算（ドラッグ中のプレビュー用）     │
└──────────────────┬──────────────────────────────────┘
                   │ Tauri IPC (invoke / channel)
┌──────────────────┴──────────────────────────────────┐
│  Rust Core                                          │
│                                                     │
│  ・geometry:  オフセット、ブーリアン、単純化、      │
│               ベジェフィッティング、検査            │
│  ・trace:     アルファ → 輪郭ポリゴン               │
│  ・matting:   ONNX 推論による背景除去               │
│  ・raster:    デコード、リサイズ、色空間変換        │
│  ・export:    PDF 生成、SVG シリアライズ            │
│  ・kkm:       ファイル形式の読み書き                │
└─────────────────────────────────────────────────────┘
```

**責務の境界**: 「対話的な操作のフィードバック」はフロントエンドで完結させ 60fps を守り、「時間のかかる確定的な計算」は Rust に投げます。ドラッグ中の切る線はフロント側の近似オフセットで描き、指を離した時点で Rust の正確な結果に差し替えます。

### 5.3 IPC の設計方針

- コマンドは全て `async`。長時間処理は Tauri の Channel で進捗を流す
- 画像バイナリを IPC の JSON に載せない。Rust 側で一時ファイルに書き、フロントには `asset://` の URL とハンドル ID を返す
- キャンセル可能な処理（AI 推論など）はトークンで管理する

```typescript
const handle = await invoke<ImageHandle>('import_image', { path });

const channel = new Channel<MattingProgress>();
channel.onmessage = (p) => setProgress(p.percent);
const cutout = await invoke<ImageHandle>('remove_background', {
  handle, model: 'isnet-general-use', onProgress: channel,
});

const cutline = await invoke<BezierPath>('generate_cutline', {
  handle: cutout, offsetMm: 3.0, smoothing: 0.5, alphaThreshold: 128,
});
```

---

## 6. データモデル

### 6.1 ドキュメント

すべての座標は **mm 単位の実寸**で保持します。ピクセルは表示時にのみ登場します。これは最も重要な設計判断です。加工機に渡すデータで最も事故が起きるのが単位系であり、Inkscape が混乱を招いている点でもあります。

```typescript
interface Document {
  version: 2;
  id: string;
  meta: { title: string; created: string; modified: string };

  /** 仕上がりサイズ。アクリル板の外形が収まる領域 */
  canvas: { widthMm: number; heightMm: number; bleedMm: number };

  /** 素材の実体。ノードから参照される */
  assets: Record<AssetId, Asset>;
  layers: Layer[];
  product: ProductSettings;
}

interface ProductSettings {
  kind: 'acrylic-stand' | 'acrylic-keychain' | 'custom';
  /** アクリル板の厚み。台座スリット幅の計算に使う */
  thicknessMm: number;      // 既定 3.0
  /** 切る線の標準的なふちの太さ */
  defaultOffsetMm: number;  // 既定 3.0
}

interface Layer {
  id: LayerId;
  name: string;
  /** 役割。書き出し時の振り分けに使う */
  role: 'artwork' | 'cutline' | 'parts' | 'guide';
  visible: boolean; locked: boolean; opacity: number;
  nodes: Node[];
}

type Node = ImageNode | PathNode | GroupNode;

interface NodeBase {
  id: NodeId; name: string;
  visible: boolean; locked: boolean;
  transform: Matrix;        // 親座標系への affine [a,b,c,d,e,f]
}

interface ImageNode extends NodeBase {
  type: 'image';
  assetId: AssetId;
  widthMm: number; heightMm: number;
  matting?: { model: string; resultAssetId: AssetId };
}

interface PathNode extends NodeBase {
  type: 'path';
  subpaths: SubPath[];      // 複数サブパスで穴あき形状を表現する
  fill: Paint | null;
  stroke: Stroke | null;
  origin?: CutlineOrigin | PartOrigin;
}

interface SubPath { closed: boolean; anchors: Anchor[] }

interface Anchor {
  p: Point;                 // アンカー点（mm）
  in: Point; out: Point;    // ハンドル（p からの相対、mm）
  kind: 'corner' | 'smooth' | 'symmetric';
}

/** 切る線の生成元。パラメータ変更で再生成できる */
interface CutlineOrigin {
  type: 'cutline';
  sourceNodeId: NodeId;
  params: CutlineParams;
  manuallyEdited: boolean;  // true なら再生成時に確認する
}

interface CutlineParams {
  offsetMm: number;         // ふちの太さ
  alphaThreshold: number;   // 0-255
  smoothing: number;        // 0=忠実 1=なめらか
  minAreaMm2: number;       // これ未満の孤立した点を無視
  keepHoles: boolean;       // 内側の穴を残すか
  joinStyle: 'round' | 'miter';
  enforceMinWidth: boolean; // 細すぎる部分を自動で太らせる
}
```

### 6.2 ファイル形式 `.kkm`

ZIP コンテナ。中身が human-readable であることを重視します（バージョン管理・デバッグ・将来の互換性のため）。

```
document.kkm  (ZIP)
├── mimetype                  # "application/vnd.kakomu.document"（無圧縮・先頭固定）
├── document.json             # 整形済み JSON
├── assets/
│   ├── <asset-id>.png        # 元画像（無改変で保持）
│   ├── <asset-id>.cutout.png # 背景を消した結果
│   └── manifest.json         # ハッシュ、元パス、EXIF
└── thumbnail.png             # 256×256（OS のファイラ向け）
```

- 元画像は必ず無改変で保持する。非破壊編集を保証するため、ファイルサイズより復元可能性を優先する
- `document.json` は整形して書き出す（`git diff` が読める）
- 未知のフィールドは読み込み時に保持し、書き出し時に復元する（前方互換）

### 6.3 もどす／やりなおす

コマンドパターン。各操作が `apply` / `revert` を持ちます。ドキュメント全体のスナップショットは取らず（画像アセットを含むためメモリを食う）、差分のみを保持します。

```typescript
interface Command {
  /** UI に表示するラベル。2 レベル分の i18n キー */
  labelKey: string;         // 例: "cmd.generateCutline"
  apply(doc: Document): Document;
  revert(doc: Document): Document;
  coalesceKey?: string;     // 連続する同種操作をまとめる
}
```

---

## 7. 機能仕様

### 7.1 F-01 絵を入れる

**入力形式**: PNG, JPEG, WebP, TIFF, SVG, HEIC（可能なら全 OS）

- ドラッグ &amp; ドロップ、ファイルメニュー、クリップボード貼り付けに対応
- 画像の DPI メタデータを読み、実寸を推定して配置する。DPI 不明なら 350dpi と仮定
- キャンバスより大きい画像は、収まるよう自動でスケールして配置
- **こまかさの警告**: 配置サイズに対する実効解像度が 300dpi を下回る場合、キャンバス上に警告バッジを出す。UV プリント出力で最も多いトラブルがこれ

**変形**: 選択ハンドルによる移動・拡縮（Shift で縦横比固定）・回転（Shift で 15° スナップ）。プロパティパネルでの数値入力（すべて mm / 度）。

### 7.2 F-02 背景を消す

**方式**: ローカル ONNX モデルによる自動セグメンテーション。ネットワーク接続を必要としません。

| UI 上の名前 | モデル | サイズ | 用途 |
|---|---|---|---|
| **写真** | `isnet-general-use` | 170MB | **既定**。写真から作るとき |
| **イラスト** | `isnet-anime` | 168MB | 手描きの絵やキャラクターから作るとき |

**「写真 / イラスト」で選ばせる理由**: 当初は「はやい / きれい / イラスト向け」の 3 択にしていたが、
「はやい」と「きれい」は<b>利用者にトレードオフの判断を求めるラベル</b>である。
何と引き換えに速いのかは、使う側には分からない。
一方「写真かイラストか」は**手元にあるものを見れば分かる**ので、初等部の子でも迷わない。
これは 3.4 の「行為の言葉で書く」と同じ考え方である。

**`isnet-anime` を用意する理由**: 利用者に初等部の子が含まれる以上、
入力は写真よりも手描きのイラストである場合が多い。写真向けのモデルは線画で崩れやすい。

**軽量モデル `u2netp`（4.4MB）** はコードには残してあるが、選択肢には出さない。
統合テストで実際に推論を通すときに使う（170MB を毎回落とさずに済ませるため）。

**ラボの共用 PC には、セットアップ時にあらかじめ全モデルを配置しておきます**（子どもに初回ダウンロードを待たせないため）。

**実行バックエンド**: macOS = CoreML、Windows = DirectML、Linux = CUDA（あれば）。いずれも CPU にフォールバック。

**処理フロー**:
```
1. 入力画像をモデル入力サイズ（1024×1024 等）にリサイズ
2. 推論 → アルファマスク（1ch, float）
3. 元解像度にアップサンプリング（bicubic）
4. エッジ処理（しきい値処理はせず、ソフトエッジを保持）
5. 元画像の RGB × マスク → RGBA PNG を新アセットとして作成
```

**UI**: ワンクリックで実行。進捗バーとキャンセルボタン。結果はビフォー／アフターのスライダーで比較できる。「もどす」で消す前に戻せる（元アセットは常に保持されている）。

**制約の伝え方**: 髪の毛、透明なガラス、白背景に白い服では失敗します。失敗を自動検知する術はないため、結果表示時に確認を促します。ひらがなモードでは「**うまく けせて いるか みてね**」と表示します。手動修正ツールは v1.5 で追加します。

### 7.3 F-03 線を編集する

**パスツール（描画）**: クリックでコーナー、ドラッグでスムーズなアンカーを追加。始点クリックで閉じる。Enter / Esc で確定。

**ノードツール（編集）**:

| 操作 | 挙動 |
|---|---|
| クリック | 点を選ぶ |
| ドラッグ（矩形） | 範囲内の点を複数選ぶ |
| 点をドラッグ | 移動（Shift で軸拘束、Ctrl でグリッドスナップ） |
| ハンドルをドラッグ | カーブの曲がり方を変える |
| Alt + ハンドル | 対向ハンドルを連動させない |
| セグメント上をダブルクリック | 点を足す |
| Delete | 点を消す（両隣のカーブを維持） |
| S / C | 選択点を smooth / corner に |

**パス操作**: 結合、分割、方向反転、ブーリアン演算（和・差・積・排他）、単純化。
**スナップ**: グリッド、他の点、パスの交点、キャンバス境界。`%` でトグル（Inkscape 互換）。

### 7.4 F-04 切る線を自動生成する

このツールの中核機能です。

**入力**: アルファチャンネルを持つ ImageNode（背景を消した後が前提だが、元から透過 PNG でもよい）

```
[1] アルファ二値化
    alpha >= threshold（既定 128）を前景とする二値マスク

[2] ノイズ除去
    モルフォロジー的クロージング（3×3, 1回）で 1px の穴を埋める
    連結成分ラベリング → minAreaMm2 未満の島を除去

[3] 輪郭抽出
    マーチングスクエアで前景の境界ポリゴンを取得
    外輪郭と内輪郭（穴）を区別し、keepHoles=false なら穴を破棄

[4] ポリゴン単純化
    Ramer-Douglas-Peucker、許容誤差 = smoothing に基づいて 0.05〜0.5mm

[5] オフセット
    Clipper2 の InflatePaths で offsetMm だけ外側に拡張
    joinStyle=round: 角を半径 offsetMm で丸める（アクリル加工では既定）
    joinStyle=miter: 角を尖らせる（miter limit = 2.0）

[6] 自己交差の解消
    オフセット結果を Union して、内部に生じたループを除去

[7] 細部の除去（この工程が本質）
    形状を -w だけ内側にオフセット → +w だけ外側に戻す（opening）
    w = 最小許容幅 1.5mm / 2
    くびれが解消された箇所は、アクリル加工で折れやすい部位なので
    この opening 結果を採用する
    単にオフセットしただけでは、尻尾やリボンの根元が折れる幅のまま残る

[8] ベジェフィッティング
    Schneider の曲線あてはめで三次ベジェ列に変換
    許容誤差 = 0.02mm
    隣接セグメントの角度差が 60° を超える点はコーナーとして分割

[9] PathNode として cutline レイヤーに追加
```

**パラメータ UI**: ひらがな／通常モードでは **「ふちの太さ」1 つだけ**をスライダーで見せます。残りは「くわしい設定」の中に畳みます。

| パラメータ | 既定値 | 範囲 | 表に出すか |
|---|---|---|---|
| ふちの太さ（オフセット） | 3.0mm | 0〜20mm | **常に表示** |
| なめらかさ | 0.5 | 0〜1 | 常に表示 |
| アルファしきい値 | 128 | 1〜254 | くわしい設定 |
| 最小面積 | 4.0mm² | 0〜100 | くわしい設定 |
| 穴を残す | OFF | — | くわしい設定 |
| 角の処理 | 丸 | 丸/尖り | くわしい設定 |
| 細すぎる部分を直す | ON | — | くわしい設定 |

**再生成**: パラメータを変えると再生成されます。ただし手動で点を編集した後（`manuallyEdited: true`）は確認を挟みます。

**手動編集への引き継ぎ**: 生成された線は通常の PathNode です。そのままノードツールで編集できます。生成 → 微調整という流れを分断しません。

### 7.5 チェック（バリデーション）

生成後に自動でチェックし、問題箇所をキャンバス上にマーカー表示します。

> **これは門番ではなく教材です。** 「創ることで学ぶ」場では、なぜこの形が切れないのかを伝えることに価値があります。エラーは必ず、**何がダメか**と**どうすれば直るか**をセットで書きます。

| チェック | 条件 | 重要度 | 通常の文言 | ひらがな |
|---|---|---|---|---|
| 最小幅 | < 1.5mm | エラー | ここが細すぎます。折れやすいので、ふちの太さを増やすか、形を変えてください | ここが ほそすぎます。おれちゃうので ふちを ふとく してね |
| 最小穴径 | < φ2mm | エラー | この穴は小さすぎて、機械の刃が入りません | この あなは ちいさすぎて きかいが はいりません |
| 自己交差 | — | エラー | 線が自分と交わっています。ここを直してください | せんが じぶんと ぶつかって います |
| 開いたパス | — | エラー | 線がつながっていません。切り抜けません | せんが つながって いません |
| キャンバスはみ出し | — | 警告 | 板からはみ出しています | いたから はみだして います |
| 絵の露出 | — | 警告 | 絵が線の外に出ています | えが せんの そとに でて います |
| 鋭角 | 内角 < 20° | 警告 | ここが尖りすぎています。欠けやすいです | ここが とがりすぎて います。かけやすいです |
| こまかさ | < 300dpi | 警告 | 絵が粗く印刷されます。もっと大きい画像を使うか、小さく作ってください | えが あらく なります。もっと おおきい えを つかってね |

### 7.6 F-05 部品をつける

パラメトリックなテンプレートとして提供します。配置後もパラメータを変更でき、その場で再生成されます。

#### キーホルダーの穴

| 項目 | 既定値 | 根拠 |
|---|---|---|
| 穴の直径 | φ4.0mm | 一般的なボールチェーン・カニカン用 |
| 縁までの最小距離 | 3.0mm | 強度確保。下回るとエラーマーカーを出す |

- 円をベジェ（4 セグメント、magic number 0.5522847498）で生成
- 配置時、切る線の内側に自動でスナップし、条件を満たす位置を提案する

#### アクスタの台座

| 項目 | 既定値 | 備考 |
|---|---|---|
| 外形 | 60 × 20mm、角丸 3mm | 楕円・台形も選べる |
| スリット幅 | アクリル厚 + 0.2mm | 厚 3mm なら 3.2mm。差し込みのクリアランス |
| スリット長 | 25mm | 端は丸める（応力集中を避ける） |
| 差し込みタブ | スリット長 − 0.4mm | 絵柄側に生成。タブ高 = 台座厚 + 1mm |

台座は絵柄と同じ板に並べるため、キャンバスの空きスペースに自動配置します。

**穴とスリットは、切る線の内輪郭（サブパス）として追加します。** 別パスにすると、加工機側でどちらを切るか判定できない場合があるためです。

### 7.7 F-06 かさね（レイヤー）

初期状態で 3 つ作られます。追加・削除はできますが、`role` は変更できません（書き出しの振り分けに使うため）。

| 名前 | role | 内容 | 書き出し先 |
|---|---|---|---|
| え | `artwork` | 入れた画像 | PDF |
| きる せん | `cutline` | 生成・編集された切る線 | SVG |
| ぶひん | `parts` | 穴、台座 | SVG |
| したじき | `guide` | 参照用の下絵など | どちらにも含めない |

### 7.8 F-12 共用 PC モード

つくまなラボの共用 PC で使われることを前提とした動作です。設定で有効化し、ラボ側であらかじめ ON にしておきます。

| 要件 | 動作 |
|---|---|
| アカウント不要 | ログインの概念を持たない。起動したら即使える |
| 前の人のデータが見えない | 起動時に新規ドキュメントで開く。最近開いたファイルの一覧を出さない |
| 持ち帰り導線 | 書き出し後に「**データを もって かえる**」を提示。USB / ダウンロードフォルダ / 印刷済みの実物のみ、を選べる |
| かたづけ | 「**おわる**」ボタンで、作業ファイルと一時ファイルをまとめて削除して終了する。確認は 1 回だけ |
| 自動クリーンアップ | 一時ディレクトリのファイルは 24 時間で自動削除 |
| 設定の固定 | ラボ管理者が既定の読みやすさレベル・キャンバスサイズ・加工機設定をロックできる |

各自のノート PC で使う場合、このモードは OFF です。通常どおり最近使ったファイルが並び、データは残ります。

---

## 8. 出力（加工機に渡す）

外部業者への入稿ではなく、**同じ部屋にある機械に渡す**ためのデータです。

### 8.1 F-07 SVG（切るデータ → レーザー加工機 xTool P3）

**実機**: xTool P3。制御ソフトは **XCS（xTool Creative Space）**。

```xml
<svg xmlns="http://www.w3.org/2000/svg"
     width="80mm" height="120mm"
     viewBox="0 0 80 120">
  <!-- ユーザー座標系 1 単位 = 1mm -->
  <g id="cutline" inkscape:label="CutContour" inkscape:groupmode="layer">
    <path d="M ..." fill="none" stroke="#FF00FF" stroke-width="0.1"/>
  </g>
</svg>
```

| 項目 | 値 | 理由 |
|---|---|---|
| 単位 | `width`/`height` に mm を明記、`viewBox` は mm と 1:1 | どのソフトで開いても実寸が保たれる |
| 線 | `fill="none"`, `stroke-width="0.1mm"` | カットラインの標準的な表現 |
| 色 | マゼンタ `#FF00FF` | CutContour の慣例色。設定で変更可 |
| レイヤー名 | `CutContour`（`inkscape:label` も併記） | Illustrator でも Inkscape でも認識される |
| 色による振り分け | **XCS は色ごとに加工方法を割り当てられる。** レイヤー名よりも色のほうが確実に効くので、色を第一の手がかりにする | XCS はレイヤー名を無視することがある |
| 塗り | `fill="none"` を必ず付ける | XCS は塗りのある図形を「彫刻」、線だけの図形を「切断」として扱う |
| transform | すべてベイクし、絶対座標にする | 加工機のソフトが transform を解釈できないケースがある |
| 曲線 | 円弧を使わず三次ベジェ（C）のみ | 互換性が最も高い |

**オプション**: 絵柄をラスタとして埋め込む（位置確認用）／部品を含める・含めない／線の色・太さ。

**切る線がまだ無いとき**: M4 の前でも実機に通せるよう、切る線が 1 本も無ければ
**板の外形（角丸の長方形）を仮に書き出します**。実寸・色・線幅が XCS で正しく解釈されるかを、
カットライン生成を作る前に確かめられるようにするためです。書き出した後に「仮である」ことを画面に出します。

### 8.2 F-08 PDF（印刷するデータ → UV プリンター Roland BD-8）

**実機**: Roland VersaSTUDIO **BD-8**。RIP は **VersaWorks 7**。CMYK に加えて**白インク**を積んでいる。

| 項目 | 値 |
|---|---|
| PDF バージョン | 1.7 |
| MediaBox | 仕上がり + 塗り足し |
| TrimBox | 仕上がりサイズ |
| BleedBox | MediaBox と同じ |
| 解像度 | 既定 600dpi（300 / 600 / 1200 / 原寸維持） |
| 圧縮 | 可逆（FlateDecode）を既定。JPEG は選択制 |
| 透過 | SMask としてアルファチャンネルを保持 |
| 色空間 | 既定 RGB（sRGB ICC 埋め込み）。CMYK 変換はオプション |

**色空間の判断**: UV プリンターの RIP は多くの場合 RGB 入力を推奨します（プリンター側のプロファイルで変換した方が色域を広く使えるため）。したがって既定は sRGB とし ICC を埋め込みます。CMYK が必要な場合は `lcms2` による変換をオプションで提供します。

### 8.3 白版（ホワイトインク層）は作らない

**RIP 側が持っている機能なので、アプリでは作らない。**

一度は「アクリルは透明だから白を敷かないと色がくすむ。BD-8 は白インクを積んでいるので
白版は必須だ」と判断し、v1 の機能として書いた。これは**実機を触らずに出した推測で、間違っていた**。

実際には、ラボでこれまで**白を含んだ画像をそのまま PDF にして問題なく印刷できている**。
VersaWorks 7 には特色版（白・グロス）を画像から自動生成する機能があり、
PDF 側にスポットカラーの版を持たせる必要がない。

**将来これを見直すとしたら**、次のどちらかが必要になったときである。

| 条件 | なぜアプリ側が要るか |
|---|---|
| 白を敷く範囲を絵柄ごとに変えたい | RIP の自動生成は不透明部分に一律で敷く。「ここだけ透明のまま見せる」といった造形はできない |
| チョーク量（白版を内側に縮める量）を自分で決めたい | 印刷のズレで白が縁からはみ出すのを防ぐ調整。RIP の既定で足りるなら不要 |

どちらも「凝った作品を作りたくなったら」の話であり、
初等部の子が 1 回の来室でアクキーを 1 つ作る導線には要らない。

### 8.4 書き出しの UI

- 書き出し先はフォルダを選ばせ、2 つ並べて置く。名前は元の画像から引き継ぐ（`<元の名前>_切る.svg` / `<元の名前>_印刷.pdf`）。何が何だか分からないファイルを作らない
- **「りょうほう かきだす」を主導線に置く。** 片方を忘れる事故を防ぐため
- 書き出し前にチェック（7.5）を走らせ、エラーがあれば止めて説明する
- 書き出し後、**次にどの機械に持っていくかを画面に出す**。「きるデータは レーザーかこうきへ」「いんさつするデータは UVプリンターへ」

---

## 9. UI 設計

### 9.1 画面構成

```
┌────────────────────────────────────────────────────────────┐
│ メニューバー              [かんじ | ひらがな]     [おわる]  │
├──────┬──────────────────────────────────────┬──────────────┤
│      │                                      │              │
│ どうぐ│           キャンバス                 │ せってい     │
│      │                                      │              │
│ [えらぶ]   ┌────────────────┐  ← 仕上がり枠  │ ・えらんだ   │
│ [てん]│    │                │     (実線)     │   ものの情報 │
│ [せん]│    │   え           │                │ ・ふちの太さ │
│ [しかく]   │                │                │ ・チェック   │
│ [まる]│    └────────────────┘  ← ふちの余白  │   けっか     │
│ [あな]│        ....................  (破線)   │              │
│ [だいざ]                                     ├──────────────┤
│      │                                      │ かさね       │
│      │                                      │ ・え         │
│      │                                      │ ・きる せん  │
│      │                                      │ ・ぶひん     │
├──────┴──────────────────────────────────────┴──────────────┤
│ 座標(ミリ) / ズーム / えのこまかさ / ちゅうい 2けん          │
└────────────────────────────────────────────────────────────┘
```

### 9.2 設計方針

**「つぎに やること」を常に提示する。** 何から始めればいいか分からない状態を作りません。キャンバスが空のときは大きなドロップゾーンと「えを いれる」だけを見せ、画像が入ったら「うしろを けす」を、消えたら「かこむ」を、それぞれ目立つ位置に出します。ウィザードではなく通常 UI 上のハイライトとして行い、上級者の邪魔をしません。

**破壊的操作を作らない。** いつでも戻れると分かっていれば、子どもは試行錯誤できます。これは学習環境として重要です。

**単位は常に mm で表示する。** ピクセルを UI に一切出しません。ひらがなモードでは「ミリ」と書きます。

**押せるものは押せるように見せる。** 低学年は「クリックできそうな見た目」の手がかりに強く依存します。フラットデザインを追求しません。

**タッチにも対応する。** タッチパネル付きの共用 PC を想定し、最小タップ領域 44×44px を守ります。

**キーボードショートカットは Inkscape 互換を基本とする。** 移行者の学習コストを下げるため。ただしショートカットを知らなくても全機能に到達できることを保証します。

### 9.3 テーマ

ライト／ダーク／システム追従。omarchy はダークテーマ主体の環境なので、ダークを一級市民として扱います（後付けにしない）。

---

## 10. クロスプラットフォーム対応

### 10.1 対応プラットフォーム

| OS | 最低バージョン | 配布形式 | 優先度 |
|---|---|---|---|
| Windows | 10 (1809) / 11 | MSI, NSIS | 高（ラボ共用 PC の想定） |
| macOS | 12 Monterey 以降 | .dmg（Universal） | 高 |
| Linux (汎用) | glibc 2.31+ | AppImage | 高 |
| Linux (Arch / omarchy) | — | AUR (`kakomu-bin`) | 高 |
| Linux (Debian/Ubuntu) | — | .deb | 中 |

### 10.2 omarchy / Wayland 固有の対応

omarchy は Arch Linux + Hyprland (Wayland) 構成であり、Tauri (WebKitGTK) には既知の落とし穴があります。

| 課題 | 対応 |
|---|---|
| WebKitGTK の DMABUF レンダラが Nvidia ドライバや一部コンポジタで描画崩れを起こす | 起動時に環境を検出し、必要なら `WEBKIT_DISABLE_DMABUF_RENDERER=1` を設定。手動上書き用の環境変数も用意する |
| 日本語 IME（fcitx5 / ibus） | `GTK_IM_MODULE` / `XMODIFIERS` を検出し、未設定なら起動時に警告する |
| ファイルダイアログ | `xdg-desktop-portal` 経由。Hyprland では `-hyprland` と `-gtk` の両方が必要な点を明記 |
| ウィンドウ装飾 | Hyprland はサーバサイド装飾を持たない。クライアントサイド装飾を自前描画する |
| HiDPI / 分数スケーリング | `wp-fractional-scale-v1` に依存。`devicePixelRatio` を信頼する |
| グローバルショートカット | Wayland では取れない。使わない設計にする |

**CI での検証**: GitHub Actions に Arch Linux コンテナのジョブを置き、ビルドと最小限の起動確認を行います。GUI の実動作確認は手動テストです。

### 10.3 パッケージング

- **コード署名**: Windows は EV 証明書、macOS は Developer ID + notarization。未署名だと初回起動でブロックされ、**ラボの利用者は確実にそこで止まります**。ただし共用 PC はラボ側で一度許可すればよいため、初期は「署名なしで配布し、回避手順をドキュメント化」→ 必要に応じて署名導入、という段階を踏みます
- **自動更新**: Tauri の updater プラグイン。AppImage と AUR は自動更新を無効化する
- **ONNX モデル**: 同梱は `u2netp` のみ。ラボ共用 PC にはセットアップ時に全モデルを配置する

---

## 11. 非機能要件

### 11.1 パフォーマンス目標

測定環境の基準は **MacBook Air M1 (8GB)** を「標準的な下限」とします。

| 操作 | 目標 | 上限 |
|---|---|---|
| アプリ起動（コールドスタート） | 1.0s | 2.0s |
| 画像インポート（4000×4000px） | 0.5s | 1.5s |
| 背景を消す（`isnet`, CPU, 4000×4000px） | 8s | 20s |
| 背景を消す（`isnet`, GPU） | 2s | 5s |
| 切る線の生成（4000×4000px） | 0.8s | 2.0s |
| 点のドラッグ中のフレームレート | 60fps | 30fps |
| ズーム／パン | 60fps | 30fps |
| PDF 書き出し（600dpi, A5） | 3s | 10s |
| `.kkm` 保存（画像 20MB） | 1s | 3s |
| 読みやすさレベルの切替 | 即時 | 100ms |

**待ち時間の見せ方**: 子どもは 10 秒の無反応を「壊れた」と判断します。3 秒を超える処理には必ず進捗と、いま何をしているかの説明を出します（「いま、うしろを けして います」）。

### 11.2 リソース

- アイドル時メモリ: 300MB 以下
- 画像 1 枚（4000×4000）編集時: 1.2GB 以下
- インストールサイズ: 150MB 以下（同梱モデル + 同梱フォント込み）

### 11.3 信頼性

- **クラッシュ耐性**: 30 秒ごとに自動保存（`.kkm.autosave`）。次回起動時に復元を提案する。ただし共用 PC モードでは、他人の作業が復元されないよう当日分のみ対象とする
- **エラー処理**: Rust 側の panic を捕捉し、UI にダイアログとして表示する。プロセス全体を落とさない。エラー文言も 2 レベルの i18n を通す
- **大きい画像への防御**: 16000×16000px を超える画像はインポート時に警告し、縮小を提案する

### 11.4 プライバシー

**未成年が利用するため、通常より厳しく設計します。**

- 画像データを外部に送信しない。AI 推論はすべてローカル
- テレメトリはデフォルト無効。有効化する場合もオプトインとし、画像データは一切含めない
- ネットワークアクセスは (1) モデルの初回ダウンロード (2) アップデートチェック の 2 つに限定し、どちらも設定で無効化できる
- 共用 PC モードでは、終了時に作業データを確実に削除する

### 11.5 アクセシビリティ

- 最小タップ／クリック領域 44×44px
- コントラスト比 4.5:1 以上（本文）
- キーボードのみで全機能に到達できる
- `prefers-reduced-motion` を尊重する
- 色だけで情報を伝えない（エラーは色 + アイコン + 文言）

---

## 12. リポジトリ構成

```
kakomu/
├── SPEC.md
├── README.md
├── LICENSE                        # MIT
├── docs/                          # GitHub Pages（公開サイト）
│   └── index.html
├── .github/workflows/
│   ├── pages.yml                  # サイトのデプロイ
│   ├── ci.yml                     # lint, test, build（全 OS）
│   └── release.yml                # タグからのリリースビルド
│
├── src/                           # フロントエンド (TypeScript / Solid.js)
│   ├── main.tsx
│   ├── app/
│   │   ├── App.tsx
│   │   ├── shortcuts.ts
│   │   └── i18n/
│   │       ├── index.ts           # 2 レベルの解決
│   │       ├── ja.json            # 通常の文言
│   │       ├── ja-hira.json       # ひらがなモードの書き下ろし
│   │       └── en.json
│   ├── document/
│   │   ├── types.ts
│   │   ├── store.ts
│   │   ├── commands/
│   │   └── serialize.ts
│   ├── canvas/
│   │   ├── Canvas.tsx
│   │   ├── renderer/
│   │   ├── hittest.ts
│   │   ├── snapping.ts
│   │   └── tools/
│   ├── panels/
│   ├── geometry/                  # フロント側の軽量ベジェ演算
│   └── ipc/
│
├── src-tauri/                     # Rust バックエンド
│   ├── src/
│   │   ├── commands/
│   │   ├── geometry/              # offset / simplify / fit / validate
│   │   ├── trace/                 # アルファ → 輪郭
│   │   ├── matting/               # ONNX 推論
│   │   ├── raster/
│   │   ├── export/                # svg.rs / pdf.rs
│   │   └── kkm/
│   ├── models/                    # 同梱 ONNX モデル
│   └── fonts/                     # 同梱 UD フォント
│
├── crates/
│   └── kakomu-geometry/           # 単体でテスト・ベンチできるようにする
│
└── tests/
    ├── e2e/
    └── fixtures/
```

---

## 13. ロードマップ

### M0: 基盤（2週間）
Tauri v2 の初期化、3 OS でビルドが通る状態。ドキュメントモデルの型定義と store。キャンバスの基本描画、ズーム・パン。選択ツール、画像の配置と変形。
**完了条件**: 画像を開いて動かして拡大縮小できる

### M1: 書き出し（2週間）
SVG 書き出し。PDF 書き出し（RGB, SMask 透過, TrimBox/BleedBox）。`.kkm` の保存・読み込み。
**完了条件**: 画像を配置して PDF と SVG に書き出せる。**ラボの実機（xTool P3 / BD-8）に一度通す**

実装メモ: PDF はライブラリを使わず自前で書いている。必要なのが
「1 ページ・画像・透過・箱」だけであり、そのぶん箱と配置行列を正確に制御できることのほうが
大事なため。版ズレや実寸のズレは刷ってからでは直せない。
色空間は当面 `/DeviceRGB`（ICC は埋め込んでいない）。VersaWorks 側の入力プロファイル設定で
足りるかを実機で確認し、必要なら sRGB の ICC を埋め込む。

### M2: 線の編集（3週間）
パスツール、ノードツール、スナップ、ブーリアン演算。
**完了条件**: 画像の周りを手動で囲み、SVG に書き出せる — Inkscape の代わりとして最低限成立する

### M3: 背景を消す（2週間）
ONNX Runtime の統合、モデルのダウンロード管理、推論パイプラインと進捗表示、ビフォー／アフター比較 UI、各 OS の GPU バックエンド対応。
**完了条件**: ワンクリックで背景が消える

### M4: 切る線の自動生成（3週間）
輪郭抽出 → オフセット → ベジェフィッティングのパイプライン。パラメータ UI とライブプレビュー。チェック（7.5）。
**完了条件**: ゴールデンパス（2.2）が通しで動く

### M5: 読みやすさと部品（3週間）
**読みやすさの切り替えを UI に戻す**（基盤とひらがなの文言は M3 の時点で用意済み。M4 までに増えたぶんを書き足す）。同梱フォントの切替。キーホルダーの穴、アクスタの台座。共用 PC モード。こまかさ警告、エラーマーカーの文言整備。
**完了条件**: 初等部の子が通しで 1 個作れる（**ラボで実地テストする**）

### M6: 配布（2週間）
各 OS のパッケージング、AUR パッケージ、自動更新、ドキュメント。ラボ共用 PC へのセットアップ手順。
**完了条件**: ラボの PC に入って、実際に使われる

**合計見積もり: 約 17 週間**（1 人でフルタイム相当の場合）

### v1.5 以降
- 手動リタッチツール（消しゴム／復元ブラシ）
- ワークショップモード
- 面付け

---

## 14. テスト戦略

| 層 | 対象 | 手段 |
|---|---|---|
| 単体（Rust） | ジオメトリ演算（オフセット、フィッティング、検査） | `cargo test`。既知の入力形状に対する期待出力を固定 |
| プロパティベース（Rust） | オフセット結果の不変条件（元形状を必ず内包する、自己交差しない等） | `proptest` |
| ベンチマーク（Rust） | 切る線の生成、PDF 生成 | `criterion`。性能目標をリグレッションテストとする |
| 単体（TS） | ドキュメントモデル、コマンド、シリアライズ | `vitest` |
| i18n | 漢字側の空値・重複、ひらがな側に孤児キーが残っていないこと | `vitest`。網羅の検査は、切り替えを戻すときに有効化する |
| ゴールデンファイル | SVG / PDF の出力 | 既知の `.kkm` から書き出し、期待ファイルと比較 |
| E2E | ゴールデンパス | `tauri-driver` + WebDriver。3 OS の CI で実行 |
| 実地 | **初等部の子による通しテスト** | M5 完了時にラボで実施。詰まった箇所をそのまま課題とする |
| 手動 | omarchy 実機での描画・IME・ファイルダイアログ | リリース前チェックリスト |

**テストフィクスチャ**: 意図的に難しいケースを揃えます。
- 髪の毛が細かいキャラクターイラスト
- 背景が白い、白い服の人物写真
- 細い突起（尻尾、リボン）を持つ形状
- 中央に穴の開いたドーナツ形状
- 半透明のグラデーションを含む PNG
- 巨大画像（8000×8000）
- **子どもが描いた、線の細い手描きイラストのスキャン**

---

## 15. リスクと未決事項

### 15.1 技術的リスク

| リスク | 影響 | 対策 |
|---|---|---|
| ベジェフィッティングの品質が実用に耐えない | 切る線が「なんとなく違う」形になる | M4 の早い段階でプロトタイプを作り、実際の加工結果と比較する。Schneider が不十分なら曲率連続性を考慮した手法に切り替える |
| ひらがなモードの言い換えが伝わらない | 低学年が使えない = 主目的の未達 | M5 の実地テストで確認する。文言はコードから分離してあるので、テスト結果を受けて素早く直せる |
| ONNX Runtime のクロスビルドが煩雑 | ビルドが不安定になる | M3 の最初に 3 OS でビルドを通す。困難なら pure Rust の `tract` へフォールバック |
| SVG DOM が要素数増で遅くなる | 大きなドキュメントで操作がもたつく | CI のベンチで監視し、超えたら Canvas2D + Path2D に切り替える |
| WebKitGTK の Wayland 周りの不安定さ | Linux で使い物にならない | M0 の時点で omarchy 実機での動作確認を行う |
| 加工機の実際の要求と出力が合わない | 出力が機械に通らない | **M1 の時点で、ラボの実機に一度通す。** 仕様書だけで決めない |

### 15.2 未決事項

| # | 事項 | 決める時期 |
|---|---|---|
| ~~Q-01~~ | ~~レーザー加工機の機種~~ → **xTool P3**（制御ソフトは XCS）と判明。[8.1](#81-f-07-svg切るデータ--レーザー加工機) を機種に合わせて更新済み | 決定済み |
| ~~Q-02~~ | ~~UV プリンターの機種と RIP~~ → **Roland VersaSTUDIO BD-8 + VersaWorks 7**。白版は RIP が自動生成するのでアプリでは作らない（[8.3](#83-白版ホワイトインク層は作らない)） | 決定済み |
| Q-03 | 切る線の既定色をマゼンタ `#FF00FF` とするか。加工機の設定に合わせる | M1 まで |
| Q-04 | キーホルダーの穴を切る線の内輪郭として持つか、別パスとして持つか。加工機の要求次第 | M1 まで |
| Q-05 | ひらがなモードの文言を誰が監修するか。初等部の教員に見てもらえるか | M5 まで |
| Q-06 | ラボ共用 PC の OS とスペック。性能目標の基準を実機に合わせる | M0 まで |
| Q-07 | 同梱モデルを `u2netp` のみとするか。共用 PC には事前配置するので、各自の PC 向けの判断 | M3 まで |
| Q-08 | macOS のコード署名を行うか（Apple Developer Program 年額 $99） | M6 まで |
| Q-09 | フロントエンドを Solid.js とするか React とするか | M0 の実装開始前 |
| ~~Q-11~~ | ~~ライセンス~~ → **MIT に決定**。同梱モデル（isnet は Apache-2.0）とも整合する | 決定済み |

---

## 16. 参考にする既存実装

| プロジェクト | 参考にする点 |
|---|---|
| Inkscape | ノードツールの操作体系、ショートカット、パス演算の挙動 |
| Figma / Penpot | 選択・変形ハンドルの UX、プロパティパネルの設計 |
| Scratch | **子ども向け UI の作り方。**ブロックの大きさ、色、押せることの見せ方、失敗しても壊れない設計 |
| rembg | 背景除去のモデル選定と前処理・後処理 |
| VTracer / potrace | ラスタからベクタへの変換手法 |
| Clipper2 | ポリゴンオフセットとブーリアン演算 |
| LightBurn | レーザー加工向けの UI。実寸表示と検証機能の見せ方 |
| UD デジタル教科書体 / BIZ UD フォント | 教育現場での可読性設計 |

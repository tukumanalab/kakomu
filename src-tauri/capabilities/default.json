{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "kakomu の既定の権限。ファイルを開くダイアログと、取り込んだ画像の表示のみ。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
